# 非 KIR 核心基线提交记录（2026-09-12）

## 范围

本次将 RLinf 已验证的 DreamDojo × GR00T N1.7 集成固定为本地 Git 基线，父提交为
`36996329487374af2b42f87d87d88cda6d0eb3c9`。提交主题为
`feat(dreamdojo): freeze native GRPO cluster baseline`。

包含此前的动作/状态桥修复、384D conditioning、milestone reward、原生 train/eval 与视频、
55例验证集去除 padding 偏差、性能开关、GR00T 新版接口兼容、独立容器构建及部署工具、
Slurm 自动续跑、WM/noise/LR/batch/seed 对比提交工具，以及相关测试和历史实验记录。
运行参数仍由既有 YAML 和 CLI 决定；本次不改变训练行为，不提交新作业。

不包含正在验证的交接 KIR：自适应选帧、按组起点、奖励预热、KIR 诊断与提交参数均留在
工作区，后续独立提交。基线的 DreamDojoEnv 仍要求 `enable_kir=false`。
日志、视频、数据、权重、虚拟环境、sqsh 和临时源码快照不纳入提交。

## 拆分依据与运行中的实验

重叠源码从 cluster 未添加 KIR 的 `code/RLinf/` 只读取回，与本地逐块比较后仅把
非 KIR 内容写入 Git 暂存区。README 保留既有 sweep/follow-up 文档链接，排除 KIR 链接。
当前工作区代码不回退，不使用 stash 或整树 checkout。测试运行于暂存内容导出的独立目录，
不以包含 KIR 的当前工作区测试代替基线测试。

以下运行相关文件保持原部署内容，SHA256 为：

| 文件 | SHA256 |
| --- | --- |
| `rlinf/envs/world_model/world_model_dreamdojo_env.py` | `c2731faa0630d7339aa02d3db0d6159f5f74c1427df886fc8412bacadb276f6e` |
| `rlinf/envs/world_model/dreamdojo_reward.py` | `ad7a3286156f8b39f129d3a475a9392e05c4a6fbd2b22c8e75a95c40dacee550` |
| `rlinf/runners/embodied_runner.py` | `83011dec591111b17c9be1c92e77b211dc93a88a8812ada858e7df95623ba985` |
| `rlinf/utils/train_budget.py` | `758b903a1734d826562ab3d9a596cdca5f264e2ba0f603e31c6e71bee4bd077d` |
| `docker/dreamdojo/chain.py` | `3af2a1c2efb2a5c374f3039df7ecc1c8d54caf535d8fff09c9765be7011fe153` |
| `docker/dreamdojo/train_chain.slurm` | `cb905b9bfa07ae97162bd934b4aca3eda8567b43accf6b62fcccd8df9f12545d` |
| `docker/dreamdojo/run_cluster.slurm` | `665cbc026ddf65bb7d85fa6a111b416b59b3821153b0f06585679b3f462f9f59` |

本轮不覆盖 cluster 的源码、续跑入口或运行配置；Git commit 本身不会改变正在执行的实验。
后续新功能应使用独立代码版本与实验名，不能覆盖已被续跑链 hash 固定的 runtime 文件。

## 外部依赖边界

- GR00T 源码：`2d9a9fce9811d10510d1263344fbf5253f33639d`；本机 `.venv/gr00t`
  与 sibling `Isaac-GR00T` 都指向此版本。本次不修改或提交这两个仓库。
- DreamDojo HEAD：`783004974e67f8eeb7792a948b2acfc453833c5f`，仍有独立的未提交修改，
  包含 RL 推理优化和严格 LAM 加载器。**RLinf 的这个 commit 不是三个仓库完整源码的冻结。**
  从纯上游 DreamDojo HEAD 重新安装不能视为复现了当前组合环境；详见
  [CLUSTER.md](CLUSTER.md)、[LAM_CHECKPOINT_AUDIT.md](LAM_CHECKPOINT_AUDIT.md)。
- 已使用镜像：`rlinf-dreamdojo-gr00t2d9a9fc-cu128-v1.sqsh`，继续配合只读补充目录
  `20260911-lam-strict` 和 `20260911-chain-v1`。共享 checkpoint 路径不变，不复制权重。
- Python/依赖版本记录在 `docker/dreamdojo/runtime-versions.json`；本轮不升级依赖或重建镜像。

## 本次验证

- 在独立导出的暂存基线上运行全部 DreamDojo CPU 回归：**114 passed，0 skipped**，
  最终一轮耗时14.65秒；4项已有 Hydra/Megatron 提示不影响结果。
- 包含 KIR 的原工作区另跑同一测试集合：**143 passed，0 skipped**，15.26秒；7项已有提示。
  这两组结果分开记录，KIR 测试不属于本次基线 commit。
- 30个待提交 Python 文件通过 Ruff lint（preview）和 format 检查；6个 Shell/Slurm
  入口通过 bash 语法检查。暂存 diff 无空白错误，未发现明显私钥/访问令牌模式。
- 3个旧文件仅执行 Ruff 换行格式整理，Python AST 完全一致：LeRobot dataset、FSDP
  strategy、GR00T simulation_io。后两者同步整理工作区；dataset 工作区已有 KIR，保持不变。
- 原有61个未提交文件中，59个 SHA256 不变；仅上述2个非 KIR 文件有纯格式变化。
  KIR 文件全部保留，暂存内容不含 handover_start_frame、prime_handover、kir_probability
  或 KIR cluster overlay。运行文件的 SHA256 与上表一致。
- 提交共60个文件，均为源码、配置、测试或文档；暂存内容与独立测试目录一致。

复验使用 RLinf 已有 Python 3.10 环境，设置 CUDA_VISIBLE_DEVICES 为空、
PYTHONDONTWRITEBYTECODE=1、OMP_NUM_THREADS=1、OPENBLAS_NUM_THREADS=1，
DREAMDOJO_ROOT 指向已打补丁的 sibling 仓库，执行 pytest -q -p no:cacheprovider
tests/unit_tests/test_dreamdojo_*.py。该 CPU 检验不会分配 GPU 或启动训练。
不把这些结果表述为新的 GPU 训练/eval 或完整统计效果验证。

本次仅本地 commit，不 push，不更改远端仓库或 Slurm 作业。
